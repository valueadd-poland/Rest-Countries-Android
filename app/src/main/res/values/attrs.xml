<?xml version="1.0" encoding="utf-8"?>

<resources>

    <eat-comment/>

    <!-- Shared -->
    <attr name="iconDrawable" format="reference"/>

    <!-- PropertyView -->

    <declare-styleable name="PropertyView">
        <attr name="iconHighlightColor" format="color|reference"/>
        <attr name="iconDrawable"/>
        <attr name="title" format="string|reference"/>
        <attr name="subtitle" format="string|reference"/>
    </declare-styleable>

    <!-- ChipIconRight -->

    <declare-styleable name="ChipIconRight">
        <attr name="iconDrawable"/>
    </declare-styleable>

</resources>
